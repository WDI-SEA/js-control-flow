let arr1 = [1, 'a', 3, 'e', 5, 7, 9, 'i'];
let arr2 = [1, 'a', 3, 'e', 5, 7, 9, 'i'];
let arr3 = [1, 'a', 3, 5, 'e', 7, 10, 'i'];
let arr4 = [0, 2, 4, 6];

let variousArr = arr4;
if (variousArr = arr2) {
    console.log(variousArr + " and " + arr2 + " are equal");
  } else if (variousArr = arr3) {
    console.log(variousArr + " and " + arr3, " are equal");
  } else if (variousArr = arr4) {
    console.log(variousArr + " and " + arr4, " are equal")
  }else {
    console.log(variousArr + " will never be equal");
  }

