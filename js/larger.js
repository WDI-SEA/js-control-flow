function larger(int1, int2) {
    if(int1 > int2) {
        console.log("the larger number if " + int1 + " and " + int2 + " is " + int1)
    } else if(int2 > int1) {
        console.log("the larger number of + ")
    } else 
    }
}