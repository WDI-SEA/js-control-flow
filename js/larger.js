const numOne = 15
const numTwo = 20

if (numOne > numTwo) {
  console.log(numOne + ' is greater than ' + numTwo)
} else if (numOne < numTwo) {
  console.log(numTwo + ' is greater than ' + numOne)
} else {
  console.log(numOne + ' must be equal to ' + numTwo + ' then')
}
