//larger.js
//Write a program that reads two Number variables and prints which one is greater. (e.g. "The larger number of 7 and 3 is 7")

let first = 480
let second = 240

if(first > second){
  console.log('The larger between '+first+' and '+second+' is '+first)
}

else if(first < second ){
  console.log('The larger between '+first+' and '+second+' is '+second)
}

else if(first === second){
  console.log('The larger between '+first+' and '+second+' is BOTH, thought u tricked me human!')
}

else {
  console.log('NA-DA')
}
