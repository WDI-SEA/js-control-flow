var firstNumber = 4;
var secondNumber = 3;


if (firstNumber >= secondNumber) {
    console.log("The larger number of " + (firstNumber) + (secondNumber) + " is " + firstNumber);
} else {
    console.log("The larger number is "  + secondNumber);
}