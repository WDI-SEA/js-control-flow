if(arr1.length !== arr2.length)
   console.log('womp, womp...these arrays are not the same.')
  else {
   for(let i = 0; i < arr1.length; i++)
   if(arr1[i] !== arr2[i])
    console.log('womp, womp...these arrays are not the same.')
  else {
    console.log('These arrays are the same!')
  }
}