let a =[1,2,"3"]
let b = [1,2,3,]
if(a.length == b.length){
    //The arrays are the same length
    for (let i =0;i<a.length;i++){
    if(a[i]!==b[i]){
       console.log("The arrays aren't the same")
       break;
        if(a[i]=a[a.length-1]){
            console.log("the arrays are the same ")
        }
    }

}   
}  
 else{
        console.log("The arrays are not the same length")
}

 