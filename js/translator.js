var multiL = ['En , Es , Fr'];
var Hello World = Math.floor(Math.random);
 let multiL = Math.floor(Math.random)[array.length];
 if multiL = Math.floor(Math.random)[array.length]; {
   console.log('Hello World'+ multiL[array.length] = 'Hello Mundo'+ 'Bonjour le monde')
 }