var thing = 'cat';
var plural = 's';
let things = thing.concat(plural);
var count = 5;
count = Math.floor(Math.random()*5 + 1)
if(count < 2){
  console.log("1 " + thing);
}
else{
  console.log(count + " " + things);
}
//code to change what the count is

//code to add an s to the end of cat depending on what the count is
