//Write a program that starts like...

//``js
//var thing = 'cat';
//var count = 5;

//and outputs the pluralized form of the word, depending
// on what `count` is. For example, "5 cats" or "1 dog". Change 
//`thing` to different values to test your code!

//**Requirements**
//* Your program should pluralize 
//the word based on an integer (`count`)

var thing = 'cat';
var count = 5;
If (count > 0); {
    console.log("cats")
} 
 if(count = 0)
  {
    console.log("cat")
}

