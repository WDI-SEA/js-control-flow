//this program takes 2 variables, a thing and a count of things, and returns a gramattically correct statment

var thing = 'cat';
var count = 1;

if (count == 1) {
	console.log(count + ' ' + thing);
} else {
	console.log(count + ' ' + thing + 's');
}

var thing = 'dog';
var count = 15;

if (count == 1) {
	console.log(count + ' ' + thing);
} else {
	console.log(count + ' ' + thing + 's');
}